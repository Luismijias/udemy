//Fórmula da área da circunferência; PI * raio * raio

let raio = 10;

let diametro = 2 * raio;
 
const PI = Math.PI;

let areaDoCirculo = PI * (raio * raio);

let resposta = `A área de um circulo cujo diâmetro é igual a ${diametro} é:  ${areaDoCirculo}`;

console.log (resposta);


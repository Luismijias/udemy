const raio = 5.6

const area = Math.PI * Math.pow(raio, 2) //pow é power

console.log (`A área de um circulo cujo o raio é ${raio} é igual à ${area}.`)

console.log (`O tipo do Math é ${typeof Math}`)